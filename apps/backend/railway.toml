[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# No custom healthcheck — Railway will just check the container is running.
# After a public domain is generated, /health becomes reachable externally.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
